import type { Options } from 'prettier';

export default {
  printWidth: 100,
  tabWidth: 2,
  singleQuote: false,
  semi: true,
  trailingComma: 'all',
  proseWrap: 'always',
  singleAttributePerLine: false,
} satisfies Options;
